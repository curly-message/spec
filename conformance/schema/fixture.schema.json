{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:curly-message:conformance:fixture",
  "title": "Curly Message Format conformance fixture file",
  "description": "One file of the conformance set: cases pinning one section of the specification at one conformance level, or cases pinning one section of CST.md against the tree an implementation offers. README.md in this package states what each field means.",
  "type": "object",
  "oneOf": [
    {
      "$ref": "#/$defs/resolutionFile"
    },
    {
      "$ref": "#/$defs/treeFile"
    }
  ],
  "$defs": {
    "level": {
      "enum": [
        "core",
        "intl",
        "extensions"
      ]
    },
    "section": {
      "description": "A heading number: a section (\"7\"), a subsection (\"9.2\") or an appendix entry (\"A.4\"). A file that pins a resolution names a heading of SPEC.md, one that pins the tree a heading of CST.md.",
      "type": "string",
      "pattern": "^([0-9]+(\\.[0-9]+)?|A\\.[0-9]+)$"
    },
    "identifier": {
      "description": "Unique across the whole set: the file's group, a slash, and a slug.",
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*/[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "case": {
      "oneOf": [
        {
          "$ref": "#/$defs/concrete"
        },
        {
          "$ref": "#/$defs/generated"
        }
      ]
    },
    "concrete": {
      "description": "A resolution written out: its inputs and what they must produce.",
      "type": "object",
      "required": [
        "id",
        "description",
        "message",
        "expected"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "$ref": "#/$defs/identifier"
        },
        "description": {
          "$ref": "#/$defs/description"
        },
        "section": {
          "$ref": "#/$defs/section"
        },
        "message": {
          "description": "The message, usually text. Any other JSON value is a message a host wrote as something else (section 4); the undefined tag is a message the caller did not supply (section 10)."
        },
        "payload": {
          "description": "The payload (section 3). Entries hold values, wrappers (section 4.1) or tagged values.",
          "type": "object"
        },
        "props": {
          "description": "The caller's formatting properties, grouped by modifier name (section 11.2).",
          "type": "object"
        },
        "locale": {
          "type": "string"
        },
        "messageId": {
          "description": "The message's id (section 4), spelled out because a case's own `id` names the case. Any JSON value: no step of resolution reads it, and reports name it (section 14.3)."
        },
        "modifiers": {
          "description": "Host-defined modifiers to register (section 11.3), each the name of a behaviour from the catalogue.",
          "type": "object",
          "propertyNames": {
            "minLength": 1
          },
          "additionalProperties": {
            "$ref": "#/$defs/behaviour"
          }
        },
        "defaults": {
          "description": "The implementation-configured defaults: the bottom formatting layer of section 11.2, grouped by modifier name.",
          "type": "object"
        },
        "expected": {
          "$ref": "#/$defs/expected"
        }
      }
    },
    "generated": {
      "description": "A case the runner builds from the limits the adapter declares (section 13); the README states each construction.",
      "type": "object",
      "required": [
        "id",
        "description",
        "generate"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "$ref": "#/$defs/identifier"
        },
        "description": {
          "$ref": "#/$defs/description"
        },
        "section": {
          "$ref": "#/$defs/section"
        },
        "generate": {
          "enum": [
            "passes-at-limit",
            "passes-over-limit",
            "output-at-limit",
            "output-over-limit",
            "output-over-limit-stops",
            "conversion-over-limit"
          ]
        }
      }
    },
    "description": {
      "description": "One sentence naming what the case pins, for the failure it would print.",
      "type": "string",
      "minLength": 1
    },
    "behaviour": {
      "enum": [
        "upper",
        "echo",
        "empty",
        "nothing",
        "raise",
        "default",
        "options",
        "props",
        "locale",
        "object"
      ]
    },
    "expected": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "output": {
          "description": "The text the resolution must produce.",
          "type": "string"
        },
        "format": {
          "description": "For a locale-dependent output: the formatting request whose result on the running host is the text the resolution must produce. The message is then the placeholder alone.",
          "$ref": "#/$defs/format"
        },
        "reports": {
          "description": "The reports the resolution must emit, in the order it emits them. Omitted, none are expected.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/report"
          }
        }
      },
      "oneOf": [
        {
          "required": [
            "output"
          ],
          "properties": {
            "output": true
          }
        },
        {
          "required": [
            "format"
          ],
          "properties": {
            "format": true
          }
        }
      ]
    },
    "format": {
      "type": "object",
      "required": [
        "api",
        "input"
      ],
      "additionalProperties": false,
      "properties": {
        "api": {
          "enum": [
            "NumberFormat",
            "DateTimeFormat",
            "RelativeTimeFormat"
          ]
        },
        "options": {
          "description": "The options the request is constructed with: the properties the facility reads, as the layers of section 11.2 compose them and the modifier pins them, without the format's own ratio and format, which the input already reflects.",
          "type": "object"
        },
        "input": {
          "description": "What the request formats: a number for NumberFormat, milliseconds since the epoch for DateTimeFormat, a [value, unit] pair for RelativeTimeFormat."
        }
      }
    },
    "report": {
      "type": "object",
      "required": [
        "code",
        "origin"
      ],
      "additionalProperties": false,
      "properties": {
        "code": {
          "enum": [
            "unknown-modifier",
            "failed-modifier",
            "missing-options",
            "unserializable-value",
            "missing-locale",
            "pass-limit",
            "output-limit"
          ]
        },
        "origin": {
          "enum": [
            "message",
            "payload",
            "limit"
          ]
        },
        "id": {
          "type": "string",
          "description": "The id the report must name, where the adapter's reports carry one. Text, and compared as written: tagged values (see the package README) are read on a case's inputs and not in its expectations, so a case whose id is not text leaves this out and the id goes unobserved."
        }
      }
    },
    "resolutionFile": {
      "description": "A file of cases that pin a resolution: what a message, over inputs, must produce.",
      "type": "object",
      "required": [
        "format",
        "level",
        "section",
        "cases"
      ],
      "additionalProperties": false,
      "properties": {
        "format": {
          "description": "The versioned identifier of the format the file targets.",
          "const": "curly-message-1"
        },
        "kind": {
          "description": "What the file pins. Omitted, it pins a resolution.",
          "const": "resolution"
        },
        "level": {
          "description": "The conformance level (section 2) that requires every case in the file.",
          "$ref": "#/$defs/level"
        },
        "section": {
          "description": "The section of SPEC.md the file pins; a case may name a more specific one.",
          "$ref": "#/$defs/section"
        },
        "cases": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/case"
          }
        }
      }
    },
    "treeFile": {
      "description": "A file of cases that pin the tree of CST.md. It declares no level: that document is not one of the conformance levels of section 2, and its cases run where the implementation offers a tree.",
      "type": "object",
      "required": [
        "format",
        "kind",
        "section",
        "cases"
      ],
      "additionalProperties": false,
      "properties": {
        "format": {
          "description": "The versioned identifier of the format the file targets.",
          "const": "curly-message-1"
        },
        "kind": {
          "description": "What the file pins.",
          "const": "tree"
        },
        "section": {
          "description": "The section of CST.md the file pins; a case may name a more specific one.",
          "$ref": "#/$defs/section"
        },
        "cases": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/treeCase"
          }
        }
      }
    },
    "nodeType": {
      "description": "A node kind of the tree (CST.md section 6).",
      "enum": [
        "message",
        "placeholder",
        "open",
        "close",
        "separator",
        "space",
        "key",
        "modifier",
        "option-key",
        "option-value",
        "text",
        "escape"
      ]
    },
    "expectedNode": {
      "description": "What a case expects of one node: what it is and what it spells, never where it is. A span is in the implementation's own unit (CST.md section 4), so a case that wrote numbers would pin one implementation's unit on every other.",
      "type": "object",
      "required": [
        "type",
        "text"
      ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "$ref": "#/$defs/nodeType"
        },
        "text": {
          "description": "The text the node spans, which the runner reads off the span the implementation answered with.",
          "type": "string"
        },
        "name": {
          "description": "For a name kind: the span unescaped (CST.md section 7).",
          "type": "string"
        },
        "cancels": {
          "description": "For an escape: which of the two readings of section 7 the sequence takes.",
          "type": "boolean"
        },
        "nodes": {
          "description": "The children, in order. Stated for every node the case reads into.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/expectedNode"
          }
        }
      }
    },
    "treeCase": {
      "description": "A message put to the implementation's tree, and the children of the root it must answer with.",
      "type": "object",
      "required": [
        "id",
        "description",
        "message",
        "expected"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "$ref": "#/$defs/identifier"
        },
        "description": {
          "$ref": "#/$defs/description"
        },
        "section": {
          "$ref": "#/$defs/section"
        },
        "message": {
          "description": "The message, as text. A tree is a description of the source, so a message a host wrote as something else has none.",
          "type": "string"
        },
        "expected": {
          "description": "The children of the message node, in order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/expectedNode"
          }
        },
        "resolves": {
          "description": "What the same message resolves to over no payload, where the case pins that the two readings of it agree (CST.md section 5, property 4). The message then names no modifier, so the expectation holds at the core level.",
          "type": "string"
        }
      }
    }
  }
}
