{
  "name": "@curly-message/site",
  "version": "0.0.0",
  "private": true,
  "description": "Source of the Curly Message Format's public site.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node build.mjs",
    "serve": "node --run build && node --watch-path=. build.mjs --serve"
  },
  "devDependencies": {
    "@curly-message/parser": "^1.0.1",
    "marked": "^18.0.12"
  }
}
